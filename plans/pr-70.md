## Summary
Implement conversation continuity for document Q&A follow-ups by adding a bounded recent-history window to prompt assembly for both `/query` and `/query/stream`.

## Linked Issue
- Closes #70

## Scope
**In scope:**
- Add recent conversation history retrieval in documents query flows
- Pass history into Anthropic answer + streaming answer generation
- Keep history ordering oldest -> newest and role formatting explicit (`User:` / `Assistant:`)
- Add/adjust backend tests for history window and ordering
- Update targeted docs (`TESTPLAN.md`, `docs/ARCHITECTURE.md`)

**Out of scope:**
- Cross-document memory or global session memory
- DB schema/migration changes
- Frontend UX redesign

## Acceptance Criteria Check
- [x] Acceptance criteria from Task issue are fully met

## Verification
```bash
make backend-verify
cd backend && .venv/bin/pytest -q tests/test_anthropic_service_logging.py tests/test_query.py
```

## Docs and Decisions
- [x] Docs updated as needed (`TESTPLAN.md`, `docs/ARCHITECTURE.md`)
- [ ] ADR created/linked if decision has lasting architecture/security/perf impact (not required for this task)

## Risks / Rollback
- Risk level: Low to medium (prompt composition behavior change)
- Rollback plan: Revert this PR to restore previous no-history prompt behavior
