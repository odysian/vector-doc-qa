## Summary
Add explicit `Stop` and `Retry` controls for streaming chat in the dashboard chat window, with a small in-flight guard that prevents duplicate stream placeholders from rapid double-submit.

## Linked Issue
- Closes #74

## Scope
**In scope:**
- `frontend/app/components/dashboard/ChatWindow.tsx`
- `frontend/app/components/dashboard/__tests__/ChatWindow.streaming.test.tsx`
- `TESTPLAN.md`

**Out of scope:**
- Backend API/contract changes
- Analytics/instrumentation changes

## Acceptance Criteria Check
- [x] Stop is available only while streaming and stops active stream updates
- [x] Retry is shown for failed/stopped assistant responses and reuses the original query
- [x] Duplicate placeholder race is prevented for rapid double-submit
- [x] Sources/pipeline-meta rendering behavior is preserved
- [x] Lifecycle tests cover done/error/abort/unmount transitions

## Verification
```bash
make frontend-verify
```

## Docs and Decisions
- [x] `TESTPLAN.md` updated with stop/retry and duplicate-submit coverage
- [ ] ADR required (not required; no lasting architecture/security/performance decision)

## Risks / Rollback
- Risk level: Low (frontend-only behavior/state changes)
- Rollback plan: Revert this PR to restore prior stream send-only behavior
