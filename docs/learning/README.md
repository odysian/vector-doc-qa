# Learning Handoffs

This directory stores per-task/spec tutoring handoffs generated after explicit reviewer verdict `APPROVED`.

Filename format:

- `YYYY-MM-DD-feature-slug-learning.md`

Required content contract:

- Copy the static `TUTORING SESSION CONTEXT` header from `KICKOFF.md` verbatim at the top.
- Fill the required sections defined in `KICKOFF.md` section `4) Required Learning Handoff Template (After APPROVED)`.
